#
# picom.conf
#

blur: {
	method = "dual_kawase";
	strength = 5;
}

opacity-rule = [
	"90:class_g = 'kitty'"	
];

# GLX backend
backend = "glx";
glx-no-stencil = true;
glx-copy-from-front = false;
use-damage = true;
glx-no-rebind-pixmap = true;
vsync = true;

fading = false;
